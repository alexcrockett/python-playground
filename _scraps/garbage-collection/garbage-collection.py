# The following script can be used to free memory
# Garbage collection frees up memory not being used by the program
# This is useful where unused memory needs to be freed for performance

# Import eh garbage collection library
import gc

# Run garbage collection
gc.collect()
